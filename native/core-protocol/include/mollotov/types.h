#pragma once

#include <map>
#include <string>
#include <vector>

namespace mollotov {

using StringMap = std::map<std::string, std::string>;
using StringList = std::vector<std::string>;

}  // namespace mollotov
