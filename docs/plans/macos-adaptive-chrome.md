Follow-up scope: center the ellipsis inside the existing AppKit button, make chrome colors respond to the currently visible WebKit page, and present History as the same anchored popover style as More.

Root causes: rotating an Auto Layout-managed NSImageView changes its frame origin; fixed selected-tab fills and fixed label/icon colors ignore page luminance; History is wired as a root sheet.

Design: draw the vertical ellipsis into a centered template image rather than rotating its view. Sample a narrow top strip of the visible WKWebView with the native snapshot API on navigation, renderer updates and throttled scroll, with one sample in flight and generation checks; no persistent scripts or timers while idle. Keep samples in memory only. A per-window appearance object smoothly interpolates one palette over 200 ms, so background, tabs, labels and icons share exactly the same transition progress. Sample the median of an explicitly sRGB 12-point top-edge strip and fall back to the normal neutral palette on Start Page or unavailable WebKit. Retain native glass beneath a translucent adaptive tint. Move History into a 320x420 anchored popover using AppKit-backed row hit targets and retain Clear, entry navigation and dismissal. Do not investigate or modify Chromium.

Verification: native ellipsis image bounds; palette contrast and shared interpolation; sRGB sample accuracy and bright-foreground rejection. Generation checks reject late snapshot callbacks. Render Work/GitHub, scroll dark and light areas in both directions, check tab/text/icon contrast and popup placement, then history entry navigation. Run Swift lint, macOS tests and Release build. Cross-provider review remains unavailable due the recorded expired Claude OAuth and insufficient GLM balance; do not claim a completed cross-provider review.


Verification: 75 macOS tests pass and strict Swift lint passes. Rendered Work/GitHub checks show a dark palette and light labels/icons, scrolling follows changing page colours, and History opens as the same anchored popup. Selecting Wikipedia from History dismisses the popup and restores light-page chrome. The ellipsis is centered in both its raster canvas regression test and the rendered toolbar. Chromium was not investigated or tested in this follow-up.
